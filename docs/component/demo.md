---
diataxis: tutorial
---

# Demo

Edit the HTML and the preview re-renders. The page loads `<telelux-transcript>`
built from this repository's source, so what you see is the current state of
the component, not the last npm release.

<HtmlPlayground />

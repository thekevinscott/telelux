# Changelog

This file is a pointer, not the record — never append entries here.

Changelog entries live in the sibling [`changelog.d/`](changelog.d/) folder:
one timestamped file per change, named `YYYY-MM-DD-<slug>.md` (UTC merge date).
Newest = highest sort order.

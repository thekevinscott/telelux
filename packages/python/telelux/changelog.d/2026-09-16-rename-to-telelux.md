Changed (**BREAKING**): the project is renamed to Telelux. The PyPI
distribution is now `telelux`, the import is `telelux`, and the SDK class is
`Telelux`. Repository URLs still point at `agent-transcript-viewer` until the
GitHub repo itself is renamed. See
[the migration fragment](../migrations.d/2026-09-16-rename-to-telelux.md).
